# Normaliza quebras de linha: LF no repositório, nativo no working copy.
* text=auto eol=lf

# Fixtures das fontes de bilhetes são baixados com curl: não normalizar.
tests/fixtures/**/*.html -text

*.png binary
*.jpg binary
*.ico binary
